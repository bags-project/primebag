{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}

    <h1>Panier</h1>
    <p>Vous avez X objets dans voter panier.</p>
    {{ dump(app) }}
    {{ session.articleName }}
    <div class="container">
        <div class="row">
{#
            {% for article in cart %}
                <div class="col-12">
                    <h3>{{ session.articleName }}</h3>
                    {#<img src="{{ article.poster }}" alt="image">
                    {{ article.price }}
                    {{ article.quantity }}#}
 {#}                   <hr>
                </div>
            {% endfor %}
  #}          
        </div>
    </div>






{% endblock %}
